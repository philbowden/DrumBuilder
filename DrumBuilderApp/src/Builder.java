public interface Builder {
    Builder type(String type);
    Builder color(String color);
    Builder diameter(int diameter);
    Builder depth(double depth);
    Builder shellType(String shellType);
}
